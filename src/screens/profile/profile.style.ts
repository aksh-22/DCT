import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  input: {
    marginTop: 20,
  },
  buttonArea: {
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
});
